function arrayAlements(array1) {
  for (let i = 0; i < array1.length; i++) {
      console.log(array1[i]);
  }

  console.log(`Array lenght: ${array1.length}`);

  return null;
}

module.exports = arrayAlements;
